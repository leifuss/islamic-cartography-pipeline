# Nixpacks config for Railway deployment (RAG server only)
# Overrides auto-detected requirements.txt with slim requirements-rag.txt
# Full pipeline deps (torch, docling etc.) are NOT needed for serving

[phases.install]
cmds = ["python -m venv --copies /opt/venv && . /opt/venv/bin/activate && pip install -r requirements-rag.txt"]
