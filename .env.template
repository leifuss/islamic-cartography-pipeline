# Zotero Web API (required)
# Create an API key at https://www.zotero.org/settings/keys
# Request read+write access so that annotation sync works from Stage 2 onward.
#
# IMPORTANT: Add ZOTERO_API_KEY as a GitHub repository secret, not here.
# Go to: Settings → Secrets and variables → Actions → New repository secret
# Name: ZOTERO_API_KEY
# Value: your key from zotero.org/settings/keys
ZOTERO_API_KEY=your_api_key_here

# Your Zotero library ID (shown on zotero.org/settings/keys next to each key)
# For personal libraries: this is your user ID (e.g. 12345678)
# For group libraries: this is the group ID shown in the group URL
ZOTERO_LIBRARY_ID=YOUR_LIBRARY_ID

# Library type: 'user' for personal library, 'group' for a shared group library
ZOTERO_LIBRARY_TYPE=user

# (Optional) Filter to a specific Zotero collection name.
# Leave blank to sync your entire library.
# COLLECTION_NAME=

# (Optional) Google Cloud Vision OCR — for scanned documents in non-Latin scripts
# GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account.json

# (Optional) LLM API keys — for translation, bibliography extraction, and AI features
# Only needed for Stage 3+ features. Costs pass through directly to your provider.
# GEMINI_API_KEY=your_key_here
# OPENAI_API_KEY=your_key_here
# ANTHROPIC_API_KEY=your_key_here
